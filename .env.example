# =============================================================================
# Research Portal Prototype — Umgebungsvariablen
# =============================================================================
# Kopiere diese Datei nach .env und passe die Werte an:
#   cp .env.example .env
# =============================================================================

# --- Spring Boot Profil ---
# Verfügbare Profile: local (H2 In-Memory), demo (H2 File), production (Oracle)
SPRING_PROFILES_ACTIVE=local

# --- Server-Port (Backend) ---
# Port, auf dem die Spring Boot API erreichbar ist
SERVER_PORT=8080

# --- Frontend-Port ---
# Port, auf dem das Angular-Frontend (via Nginx) erreichbar ist
FRONTEND_PORT=4200

# --- Java-Laufzeitoptionen ---
# Speicherlimits und JVM-Flags für den Backend-Container
JAVA_OPTS=-Xmx512m

# --- Logging (optional) ---
# Log-Level für Root-Logger und Applikation
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_CH_ZKB_RESEARCH=INFO

# --- Datenbank (nur für Profil 'production') ---
# Oracle-Verbindungsdaten, nur relevant wenn SPRING_PROFILES_ACTIVE=production
# SPRING_DATASOURCE_URL=jdbc:oracle:thin:@//host:1521/service
# SPRING_DATASOURCE_USERNAME=research_app
# SPRING_DATASOURCE_PASSWORD=geheim
